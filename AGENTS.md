# AGENTS.md

## 项目意图

本仓库是 DARIA STUDIO 的公开展示仓库，用于 wiki、原型展示、公开样片素材和 GitHub Pages 部署。

未来真实产品方向包括：

- 面向客户的网站。
- 面向工作人员的后台。
- 后端 API。
- 基于数据库的内容管理系统。

上述真实产品代码不应放在本公开仓库中，应放在私有仓库 `daria-studio-platform`。项目目前仍处在正式框架和数据库设计之前。不要在架构、数据库结构、认证方式和部署方式确定前急着编写正式业务代码。

## 仓库边界

- `wiki/` 存放产品文档、用户故事、验收标准、测试计划、决策记录和原型，具体规则见 `wiki/AGENTS.md`。
- `wiki/prototype/` 存放原型产物，具体规则见 `wiki/prototype/AGENTS.md`。
- `wiki/prototype/high-fidelity/` 存放当前可运行的高保真原型，具体规则见 `wiki/prototype/high-fidelity/AGENTS.md`。
- `sample_pic/` 临时存放公开样片素材，后续上线前需要复核授权、隐私和是否移入私有资产库。
- 正式客户网站、工作人员后台、后端 API、CMS、数据库、认证和部署实现放在私有仓库 `daria-studio-platform`。

## 全局工作边界

- 修改任何目录前，先阅读从仓库根目录到目标目录之间最近的 `AGENTS.md`。
- 子目录 `AGENTS.md` 可以细化父级规则，但不能违背父级项目方向和安全约束。
- 除非用户明确要求，或用户提供了需要处理的外部路径，否则默认工作范围不要向上超过最近的 `AGENTS.md` 所在父级文件夹。
- 如果任务需要跨出当前边界，先确认这是用户请求本身所要求的范围，而不是主动扩大范围。
- 不要在本公开仓库新增正式应用代码目录；如需真实开发，先切换到私有仓库。

## 全局决策原则

- 原型与正式代码必须保持分离。原型可以指导产品讨论，但不能直接被当成正式架构继续扩展。
- 正式开发在私有仓库中进行；正式开发前，需要先确认前端框架、后端框架、数据库结构、认证模型和部署模型。
- 优先采用可维护、数据驱动的内容管理方式，而不是继续硬编码功能。
- 未来价格、套餐、服务地区、服务类型、学校、场景类型、服装、道具和妆造都应建模为可编辑内容。
- 客户页面、工作人员后台、API 边界和权限边界必须清晰分离。

## Git 与安全规则

- 修改前和结束前都要检查 Git 状态。
- 不要删除或覆盖用户提供的素材和已有工作，除非用户明确要求。
- 改动范围应聚焦当前请求。
- 不要提交生成的依赖目录或构建产物，例如 `node_modules/` 或 `dist/`。
- 涉及部署配置时，需要确认 GitHub Pages 的工作目录、缓存路径和产物路径一致。
